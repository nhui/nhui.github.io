import React from "react"
import { Link } from "gatsby"
import styled from "@emotion/styled"
// import { css } from "@emotion/core"

const Inner = styled("h3")`
    display: inline-block;
`
export default ( props ) => (
    <Inner>Nathan Hui</Inner>
)
